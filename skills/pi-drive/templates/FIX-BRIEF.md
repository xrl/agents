Your working directory is `<repo workspace dir>/`; every relative path resolves from it. Set reasoning to high. You are a fresh implementer for <repo> PR #<n> (`<branch>`, worktree `<worktree>`, head `<sha>`, open at <url>, CI <state>). Work only in that worktree; never touch the primary checkout.

Read, in this order, nothing else first: `<packet>/RUST-GUIDELINES.md`; `<packet>/execution/decisions-<date>.md` D<a>–D<b> (the settled contracts; do not relitigate); `<packet>/execution/<review>.md` in full (an independent adversarial review of the PR; its F1–F<n> are your work list, its "Fix (smallest)" lines are the spec). Do not read `<packet>/execution/stage-*` or `*verifier*` files.

Do: apply F1 (blocking: <one-line restatement>, with edge and one-past tests mirroring <existing test name>), then F2–F<n> as written; where a low finding offers a choice, pick the one that keeps the contract in D<a>–D<b> and record why in a `Driver decisions` list in your final report. No WIT, wire-frame, config-key or dependency change; if a fix would need one, stop and write `<packet>/execution/<topic>-blockers.md` instead. No shims, no `#[deprecated]`, no ignored tests, no `--test-threads=1`.

Gate: scoped `cargo test -p` for the crates you touch while iterating, then once, in the background to a log file under `<packet>/execution/<topic>-gate/`, the full workspace gate from `docs/development.md` (<the gate list>). Plain `cargo`, always `--locked`; never `RUSTC_WRAPPER`, `CARGO_TARGET_DIR`, `CARGO_INCREMENTAL`, `cargo clean`. Fetch fixtures first if missing: `<fixture command>`.

Commit as one conventional commit (no `Co-Authored-By`), never `git add .`, never commit `.pi/`, the `<packet>` link, fixtures or targets. Push to the PR branch, then `gh pr checks <n> --watch`; if a check fails, read its log, fix, re-gate, push again (at most two rounds, then stop and report). Do not merge. Report: head SHA, per finding what changed with `file:line`, the gate log path and result, every CI check and result, Driver decisions, what you left out and why.
