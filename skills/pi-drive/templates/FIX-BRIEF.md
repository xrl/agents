Your working directory is `<repo workspace dir>/`; every relative path resolves from it. Set reasoning to high. You are a fresh implementer for <repo> PR #<n> (`<branch>`, worktree `<worktree>`, head `<sha>`, open at <url>, CI <state>). Work only in that worktree; never touch the primary checkout.

Read, in this order, nothing else first: `<packet>/RUST-GUIDELINES.md` (or the repo's `AGENTS.md` §Rust guidelines); `<packet>/pi/decisions.md` D<a>–D<b> (the settled contracts; do not relitigate); `<packet>/execution/<review>.md` in full (an independent adversarial review of the PR; its F1–F<n> are your work list, its "Fix (smallest)" lines are the spec). Do not read `<packet>/execution/stage-*` or `*verifier*` files.

Do: apply F1 (blocking: <one-line restatement>, with one test named for the invariant it restores, mirroring <existing test name>), then F2–F<n> as written; where a low finding offers a choice, pick the one that keeps the contract in D<a>–D<b> and record why in a `Driver decisions` list in your final report. No WIT, wire-frame, config-key or dependency change; if a fix would need one, stop and write `<packet>/execution/<topic>-blockers.md` instead. No shims, no `#[deprecated]`, no ignored tests, no `--test-threads=1`.

Don't write: signed or wider types so invalid values "join the semantic errors" (use the tight type, `NonZeroU32`); ceiling/one-over test twins, 1-ns timeout caps, ±EPSILON edges; byte budgets on error excerpts; redaction that blanks the whole body (strip controls → replace exact secrets longest-first → truncate; drop a trailing partial secret if the read was cut); canonicalized production bytes for a golden (compare parsed `Value`s); wall-clock assertions. Unknown events, items and fields in an external wire format are ignored, not errors. A fix that deletes code beats one that adds it.

Gate: scoped `cargo test -p` for the crates you touch while iterating, then once, in the background to a log file under `<packet>/execution/logs/`, the full workspace gate (`bash <packet>/pi/gate.sh`, or <the gate list> from `docs/development.md`). Plain `cargo`, always `--locked`; the machine's Rust rules are in `~/.pi/agent/AGENTS.md`. Fetch fixtures first if missing: `<fixture command>`. Never delete the worktree's `target/`; the supervisor does that at hand-off.

Commit as one conventional commit (no `Co-Authored-By`) whose body says why each change was made, in prose: no F- or D-numbers, no paths on this machine. Never `git add .`, never commit `.pi/`, the `<packet>` link, fixtures or targets. Then one fresh `lane-verifier` pass over `<sha>..HEAD` (`async: false`, saved to `<packet>/execution/fix-verify-1.md`); push only on `ACCEPT`, at the SHA its `Reviewed:` line names. Refresh the PR body if it describes what changed: a short summary plus the checks run at the pushed head, no D-numbers, no local paths. Then `gh pr checks <n> --watch` in the background, polled; if a check fails, read its log, fix, re-gate, re-verify, push again (at most two rounds, then stop and report). Do not end your turn while a subagent or background job is still running. Do not merge. Report: head SHA, per finding what changed with `file:line`, the gate log path and result, the verifier verdict, every CI check and result, Driver decisions, what you left out and why.
